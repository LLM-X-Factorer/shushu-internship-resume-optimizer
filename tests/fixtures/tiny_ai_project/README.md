# Tiny AI Project

Minimal fixture for internship-material processing tests.

Run training:

```bash
python train.py --epochs 1
```

Run evaluation:

```bash
python eval.py --checkpoint checkpoints/tiny.pt
```
